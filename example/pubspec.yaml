name: example
description: Example app for flutter_body_part_selector package
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.10.4

dependencies:
  flutter:
    sdk: flutter
  flutter_body_part_selector:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  
  # Assets from the package are automatically available via packages/ prefix
  # No need to declare them here when using path dependency
